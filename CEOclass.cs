namespace notCorporate
{
    public class CEOclass
    {
        private string _namePeramount;
        private static readonly Array workers = new Array[int.MaxValue];
        private static Array SeeHiredHand() { return workers;  }

    }
}
